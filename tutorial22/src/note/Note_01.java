package note;
/*
 * Collection : 컬렉션
 * 1. 자료구조
 *    - 변수
 *    - 배열
 *    - 구조체(c, c++) : 서로 다른 자료형을 묶어서 관리
 *    - 클래스(class) : 서로 연관된 자료를 묶어서 하나의 이름으로 관리하는 자료구조
 * 
 * 2. 가변길이 배열 : Vector(벡터),ArrayList, Linked List
 * 
 * 3. 종류
 *    - List(리스트) : 순서를 가지고 있다. 중복이 가능
 *      ArrayList, Linked List, Stack, Vector...
 *    - Set(집합) : 순서가 중요하지 않다 중복이 불가능. 
 *      HashSet, TreeSet,.....
 *    - Map(맵) : 쌍(key, value)으로 자료를 나타낸다.
 *                key는 중복이 불가능. value는 중복이 가능.
 *      HashMap  
 */
public class Note_01 {
	
}
